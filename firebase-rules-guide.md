# Firebase Realtime Database Rules

## ⚠️ خطای WebSocket 

اگر این خطا را می‌بینید:
```
WebSocket connection to 'wss://...' failed: WebSocket is closed before the connection is established.
```

**نگران نباشید!** این خطا عادی است و کد به صورت خودکار از REST API استفاده می‌کند.

---

برای اینکه نمونه کارهای شما در GitHub Pages نمایش داده شود، باید Firebase Database Rules را به این صورت تنظیم کنید:

## مراحل تنظیم:

1. به Firebase Console بروید: https://console.firebase.google.com/
2. پروژه `iliya-official` را باز کنید
3. از منوی سمت چپ، **Realtime Database** را انتخاب کنید
4. روی تب **Rules** کلیک کنید
5. Rules زیر را جایگزین کنید:

```json
{
  "rules": {
    ".read": true,
    ".write": "auth != null",
    "about": {
      ".read": true,
      ".write": "auth != null"
    },
    "blogs": {
      ".read": true,
      ".write": "auth != null"
    },
    "portfolio": {
      ".read": true,
      ".write": "auth != null"
    },
    "contact": {
      ".read": true,
      ".write": "auth != null"
    }
  }
}
```

## توضیحات:

- `.read: true` → همه می‌توانند داده‌ها را بخوانند (بدون نیاز به لاگین)
- `.write: "auth != null"` → فقط کاربران لاگین شده (ادمین) می‌توانند بنویسند

## بعد از تنظیم:

1. روی دکمه **Publish** کلیک کنید
2. صفحه GitHub Pages را رفرش کنید
3. F12 را بزنید و Console را چک کنید

## خطاهای رایج:

### اگر خطای "Permission Denied" می‌بینید:
- Rules را دوباره چک کنید
- مطمئن شوید که Publish کرده‌اید
- چند دقیقه صبر کنید (ممکن است کمی طول بکشد)

### اگر داده‌ها نمایش داده نمی‌شوند:
- Console را چک کنید (F12)
- دنبال این پیام‌ها باشید:
  - `🔍 Attempting to load portfolio from Firebase...`
  - `🔥 Firebase portfolio data:`
  - `❌ Portfolio load error:` (اگر خطا وجود دارد)
